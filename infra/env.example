# =============================================================================
# TCC IoT Platform — Variáveis de ambiente
# Copie este arquivo para .env e preencha os valores
# NUNCA commite o .env no git
# =============================================================================

# Identificador desta localidade (usado nos tópicos MQTT e nos eventos)
LOCATION_ID=ufba

# Ambiente (development | production)
NODE_ENV=development

# -----------------------------------------------------------------------------
# PostgreSQL
# -----------------------------------------------------------------------------
POSTGRES_DB=edge_db
POSTGRES_USER=edge_user
POSTGRES_PASSWORD=TROQUE_ESTA_SENHA

# -----------------------------------------------------------------------------
# InfluxDB
# -----------------------------------------------------------------------------
INFLUXDB_USER=admin
INFLUXDB_PASSWORD=TROQUE_ESTA_SENHA
INFLUXDB_ORG=tcc-iot
INFLUXDB_BUCKET=sensor_readings
# Token longo e aleatório — gere com: openssl rand -hex 32
INFLUXDB_TOKEN=GERE_UM_TOKEN_SEGURO

# -----------------------------------------------------------------------------
# MQTT — Credenciais do Edge Server (acesso total à localidade)
# Deve corresponder ao usuário configurado no mosquitto/passwd
# -----------------------------------------------------------------------------
MQTT_EDGE_USERNAME=edge-server
MQTT_EDGE_PASSWORD=TROQUE_ESTA_SENHA

# -----------------------------------------------------------------------------
# MQTT — Credenciais do Simulador (apenas para stress test)
# -----------------------------------------------------------------------------
MQTT_SIM_USERNAME=simulator
MQTT_SIM_PASSWORD=TROQUE_ESTA_SENHA

# -----------------------------------------------------------------------------
# Deduplicação (Redis)
# Janela de tempo para considerar uma mensagem duplicada
# -----------------------------------------------------------------------------
DEDUP_TTL_SECONDS=30

# -----------------------------------------------------------------------------
# InfluxDB — Batch write
# Acumula leituras antes de escrever (performance em volume alto)
# -----------------------------------------------------------------------------
INFLUXDB_BATCH_SIZE=50
INFLUXDB_FLUSH_INTERVAL_MS=5000

# -----------------------------------------------------------------------------
# ML Service
# -----------------------------------------------------------------------------
ML_ANOMALY_CONFIDENCE_THRESHOLD=0.85
ML_WINDOW_SIZE=20

# -----------------------------------------------------------------------------
# Notification Service
# Cooldown evita flood de alertas do mesmo sensor/métrica
# -----------------------------------------------------------------------------
ALERT_COOLDOWN_SECONDS=60

# -----------------------------------------------------------------------------
# Actuator Service
# -----------------------------------------------------------------------------
ACTUATOR_CONFIRM_TIMEOUT_MS=5000
ACTUATOR_MAX_RETRIES=3

# -----------------------------------------------------------------------------
# Feature flags — ativa/desativa módulos sem alterar o código
# -----------------------------------------------------------------------------
ENABLE_ML=true
ENABLE_NOTIFICATIONS=true
ENABLE_ACTUATORS=true

# -----------------------------------------------------------------------------
# Stress test (docker-compose.test.yml)
# -----------------------------------------------------------------------------
SENSORS_PER_INSTANCE=10
PUBLISH_INTERVAL_MS=500
ANOMALY_INJECTION_RATE=0.05
